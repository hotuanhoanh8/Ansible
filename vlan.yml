---
- name: tao vlan
    host: all
    gather_facts: no

    tasks:
      - name: Create vlan
        ios_vlan:
          vlan_id: "{{item}}"
          name: test-vlan "{{item}}"
          state: present
        loop:
          -10
          -11
          -12
          -13
          -14
