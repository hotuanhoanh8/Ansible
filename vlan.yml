---
- name: tao vlan
  hosts: all
  gather_facts: no

  tasks:
    - name: Create vlan
      ios_vlan:
        vlan_id: "{{ item.number }}"
        name: "{{ item.regex }}"
        state: present
      with_items:
        - { number: "23", regex: "phong_1" }
        - { number: "24", regex: "p1" }
