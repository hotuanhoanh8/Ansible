---
- name: tao vlan
  hosts: all
  gather_facts: no

  tasks:
    - name: Create vlan
      ios_vlan:
        vlan_id: "{{ item }}"
        name: "{{ item }}"
        state: present
      loop:
        - vlan 11
        - vlan 12
        - vlan 13
        - vlan 14
        - vlan 15
