---
- name: Lien ket voi file example_vars.yml
  host: all
  gather_facts: false
  vars_file:
    - example_vars.yml

  tasks:
    - name: in ten cong ty
      debug:
        msg= "Hello {{ company_name }}"

    - name: in ten nhom
      deubg:
        with_items: "{{quotes}}"
        msg= "{{item}}"
